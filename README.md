# Warden

Harden pipelines mixing trusted instructions with untrusted data. Built on `keel` trust primitives: instruction/data isolation, frozen policies, provenance, and tamper-evident audit logs. Uses the unified `checkTextRules` 15-line pure scanner.
